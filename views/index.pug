doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/tailwind.css')

body(class="font-mono bg-yellow-200")

  div(class="flex flex-col items-center")
    div(class="flex bg-blue-300 text-3xl p-2 justify-between w-full")
      h1(class="") #{title}
      h1 ...Rrrretro
    div(class=" w-64 items-cente border-solid border-black")
      - for(i=0; i <messages.length; i++){
        div(class="mt-8")
          div(class=" p-2 bg-gray-100 h-24 min-h-24 border-2 border-solid border-black")= messages[i].text
          div(class=" float-right") from #{messages[i].user}
      -}
      form(method=GET,action='/new')
        button(type=submit, class=" mySpecialClass mt-6 p-1") New Message
    


